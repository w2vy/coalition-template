# NEVER commit your manifest signing private key. It is generated by the sign step
# and must stay only on your machine.
manifest-key.pem
*.pem

# Local-only secret overrides for testing. Real secrets go in the Flux encrypted
# deployment env, never in the repo.
.env
.env.local

# Build artifacts
node_modules/
